## Pivot Chart `Preview`

### Features
{:#pivotchart-features}

* \#156894 - Provided support for updating `PivotChart` data while updating the values in underlying datasource.
* \#157031 – Provided support for sorting the `PivotAxis` and `PivotLegends` programmatically.
* \#157031 – Provided support for event to handle or listen the action performed through `PivotSchemaDesigner`.

### Bug Fixes
{:#pivotchart-bug-fixes}

* \#156894 - PivotChart is now working properly while adding the empty values in `PivotLegend` 
* \#157031 - The performance has been improved while reloading the PivotChart using `ReloadChartData` method.