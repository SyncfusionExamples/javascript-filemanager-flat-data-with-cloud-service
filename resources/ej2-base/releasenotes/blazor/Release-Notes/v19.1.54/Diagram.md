##  Diagram

###    New Features

- `## 285393` - Support to avoid connector overlapping with complex hierarchical layout has been added.
- `## 314220` - Support for Auto scrolling feature while using drawing tool has been added.

###    Bug Fixes

- `## 316115` - An exception occurs when save and load the diagram with PreventDefaults as "true".
- `## 314939` - An exception occurs when render the complex HTML template in diagram's node.
- `F163266` - When change the node's property at runtime is not working in the blazor webAssembly.
- `## 319545` - The issue with "Connector Property not updated at run time in blazor web assembly" has been fixed.
