## Gantt Chart

### Bug fixes

- Issue on NullReferenceException when switching the tab has been fixed.
- `SF-354483`-Issue while changing the splitter view using `SetSplitterPositionAsync()` method, after collapse any pane has been fixed.
