## DropDownList

### Bug fixes

- `#I389905` - Issue with "Value is not bind to the component dynamically when the provided value is beyond to the take property" has been resolved.

- `#I387784` - Issue with "preselect value does not update to the component when the TValue and TItem provided as `ValueTuple` type" has been resolved.

- `#I383980` - Issue with "Focus not added properly after press the tab key inside the filtering popup open in `DropDownList` and `MultiSelect`" has been resolved.