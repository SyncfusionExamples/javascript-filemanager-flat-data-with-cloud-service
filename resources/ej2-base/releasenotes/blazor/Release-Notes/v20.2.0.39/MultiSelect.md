## MultiSelect

### Bug fixes

- `#I389108` - Issue with "Placeholder and aria-placeholder attribute maintain as empty when placeholder set as null" has been resolved.

- `#I383980` - Issue with "Focus not added properly after press the tab key inside the filtering popup open in `DropDownList` and `MultiSelect`" has been resolved.