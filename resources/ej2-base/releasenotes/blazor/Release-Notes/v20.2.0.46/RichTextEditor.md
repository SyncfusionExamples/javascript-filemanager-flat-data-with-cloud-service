## Rich Text Editor

### Bug Fixes

- `#I397876` - Now, when the `Display` property in the `InsertImageSettings` is set as `Inline` the image is aligned to the left of the editor when inserted or pasted.

- `#I396288` - Now, copying and pasting content from Excel will work properly.