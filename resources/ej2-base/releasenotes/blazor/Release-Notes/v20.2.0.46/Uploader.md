## Uploader

### Bug Fixes

- `#I398095` - The issue “Unable to upload an outlook mail by drag and drop” has been resolved.

- `#I389640` - The issue "File doesn't upload when using `SequentialUpload` as true and `ShowFileList` as false" has been resolved.