## DatePicker

### Features

- `#I379327` - Provided support for full screen calendar in mobile mode, which supports both landscape and portrait orientations. We can enable the feature by adding `e-popup-expand` to the `CssClass` property.