## Diagram(Classic)

### Bug fixes

- `#I389422` - The accessibility violation will no longer be thrown with the aria-label attribute while running the accessibility test.
- `#I401549` - Now, the palette symbols are rendered correctly when adding symbols in the `OnInitializedAsync` with a delay.
