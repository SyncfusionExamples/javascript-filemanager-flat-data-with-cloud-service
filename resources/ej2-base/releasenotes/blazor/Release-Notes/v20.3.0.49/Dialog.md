## Dialog

### Bug fixes

- `#F177584` - Now, codes after the PromptAsync work properly from the initial render when we use 'childContent' in the predefined dialog.

- `#I407983` - Now, the `ShowAsync` method works properly when calling the method after a task delay with `ConfigureAwait(false)`.