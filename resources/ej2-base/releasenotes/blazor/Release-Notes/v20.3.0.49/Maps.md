## Maps

### Bug fixes

- `#I407383` - When a click action is performed on a marker template, the `OnMarkerClick` event will be triggered properly.
- Navigation lines in polyline structures with the same points will now render properly.