## HeatMap

### Bug fixes

- `#F178083` - The `XLabel`, `YLabel`, `XValue` and `YValue` properties in the argument of the `CellClicked` event will now be proper.

### Features

- `#I411834` - `Height` property is exposed in the event argument of the `CellRendering` event.
