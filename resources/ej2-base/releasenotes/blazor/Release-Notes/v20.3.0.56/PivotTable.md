## Pivot Table

### New features

- The popup field list will now be displayed programmatically by using the `ShowFieldListDialogAsync` method.
- `#I407080` - The pivot table cell values will now be customized and exported to Excel format using the `EnginePopulated` event.