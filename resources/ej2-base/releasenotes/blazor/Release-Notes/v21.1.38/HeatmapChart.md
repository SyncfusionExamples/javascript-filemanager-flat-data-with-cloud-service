## HeatMap

### Bug fixes

- `#I426860` - When the cell size is less than the label size, the cell labels will now render properly.
- When the legend is toggled or a selection is performed on the cells, the cells will now render properly.