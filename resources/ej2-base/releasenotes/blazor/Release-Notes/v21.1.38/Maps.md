## Maps

### Bug fixes

- `#I442504`, `#F180869` - When the Maps' bounds are set to the `ZoomToCoordinates` method as a parameter, the map will render correctly.
- `#I446875`, `#F181132` - The map now renders properly when the `ZoomFactor` property is changed dynamically.
- `#I439969` - Data label templates are now properly rendered for all projection types.
- `#I446890` - The **LineString** geometry now renders clearly when panning the Maps in the Firefox browser.