## Chart

### Bug Fixes

- `#I459075` - OnZoomEnd event's argument name is now proper when the reset button on the zoom toolbar is clicked.
- `#I461521` - Axis labels now render properly when zooming on the smaller portion of the chart.
- `#I458885` - Crosshair now renders properly while the chart is zoomed.
- `#I465217` - OnZoomStart and OnZoomEnd events now trigger properly when using the mouse wheel to zoom.
