## FileUpload

### Bug Fixes

- `#I457222` - Resolved the Console error that occurs when dragging and dropping a folder onto the file uploader.

- `#I462268` - Resolved the issue where the Beforeupload event was being called multiple times when the AllowMultiple property was set to false and SequentialUpload property was set to true.