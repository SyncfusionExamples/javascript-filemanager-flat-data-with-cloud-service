## Pivot Table

### Bug fixes

- `#I462029` - Resolved issues with localization and formatting are not applied properly when the pivot table is bound with a single value (i.e., measure) field.
- `#I463983` - The row header cell information is now properly displayed as a grand total when exporting the pivot table to an Excel document.
- `#I463983` - The issue of "0.FormattedText text" appearing in the first column header when exporting the pivot table to an Excel document has now been resolved.