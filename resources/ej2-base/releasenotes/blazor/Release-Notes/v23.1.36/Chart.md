## Chart

### Features

- `#I454382` - Provided support to get the X-axis and Y-axis values as arguments in the `OnCrosshairMove` event.
- `#I455017` - Provided support to get the `Base64` string when exporting the chart component in `SVG`, `JPEG`, `PNG`, and `PDF` formats.
- `#I468192` - Provided support to prevent the overlapping of Y-axis labels.
