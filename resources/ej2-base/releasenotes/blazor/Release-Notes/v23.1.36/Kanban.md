## Kanban

### Bug Fixes

- `#I492361` - Now, the dialog is opened properly when swimlane is enabled with an empty row.

- `#I494126` - Now, inserting data into the Kanban component through the WebApiAdaptor with an empty value works properly.