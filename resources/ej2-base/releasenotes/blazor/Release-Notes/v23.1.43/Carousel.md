## Carousel

### Bug fixes

- `#F47577` -  An issue with the `Carousel` component that occurred when both `AutoPlay` and `loop` were disabled, `AutoPlay` would become enabled after first navigation has been fixed.